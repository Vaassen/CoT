country_decisions = {
    glade_colonisation = {
        potential = {
			capital_scope = { superregion = deepwoods_superregion }
            deepwoods_outward_gladeway = {
                is_empty = yes
                NOT = { has_province_modifier = cant_colonize }
                NOT = { has_province_modifier = krak_frozen_wasteland }
                any_neighbor_province = {
                    owned_by = ROOT
                }
            }
		}

        allow = {
            total_development = 100
            treasury = 100
        }

        effect = {       
            add_treasury = -100
            random_province = {
                limit = {
                    province_group = deepwoods_outward_gladeway
                    NOT = { has_province_modifier = cant_colonize }
                    NOT = { has_province_modifier = krak_frozen_wasteland }
                    is_empty = yes
                    any_neighbor_province = {
                        owned_by = ROOT
                    }
                }
                add_siberian_construction = 100
            }
        }
    }

    deepwoods_surroundings_exploration ={
        potential = {
            has_reform = embrace_the_outside_world
            not = {
                and = {
                    has_country_flag = explored_s_castanor
                    has_country_flag = explored_folly
                    has_country_flag = explored_ourdia
                    has_country_flag = explored_harpy_hills
                }
            }
        }
        allow = {
            treasury = 50
        }
        effect = {       
            add_treasury = -50
            country_event = {
                id = deepwoods.53
                tooltip = deepwoods_decision_gather_expedition.tt
            }
        }
    }
	
	cot_upgrade_scepter = {
		potential = {
			tag = I45
			has_country_flag = meweri_scepter
		}
		
		allow = {
			treasury = 2500
			adm_power = 250
			dip_power = 250
			adm_tech = 12
			personality = mage_personality
		}
		
		effect = {
			add_treasury = -2500
			add_adm_power = -250
			add_dip_power = -250
			set_country_flag = meweri_scepter_upg
			set_country_flag = meweri_scepter_fey
		}
	}
			
	
	cot_return_to_fey = {
		potential = {
			tag = I45
			has_country_flag = meweri_scepter_upg
			NOT = { has_country_flag = meweri_scepter_fey }
		}
		
		allow = {
			tag = I45
			has_country_flag = meweri_scepter_real
			mil_power = 50
		}
		
		effect = {
			clr_country_flag = meweri_scepter_real
			set_country_flag = meweri_scepter_fey
			add_mil_power = -50
		}
	}
	
	cot_return_to_real = {
		potential = {
			tag = I45
			has_country_flag = meweri_scepter_upg
			has_country_flag = meweri_scepter_fey
		}
		
		allow = {
			tag = I45
			has_country_flag = meweri_scepter_fey
			dip_power = 50
		}
		
		effect = {
			clr_country_flag = meweri_scepter_fey
			set_country_flag = meweri_scepter_real
			add_dip_power = 50
		}
	}
}