country_decisions = {
	deranne_tales_decision = {
		potential = {
			is_or_was_tag = { tag = A02}
			mission_completed = deranne_home_of_tales_and_piety
		}

		provinces_to_highlight = {
		}

		allow = {
			NOT = { has_country_flag = deranne_chose_tale_recent }

		}

		effect = {
			custom_tooltip = deranne_tales.tt
			country_event = {
				id = flavour_deranne.19
			}
		}
	}
	deranne_dam_decision = {
		potential = {
			is_or_was_tag = { tag = A02 }
			mission_completed = deranne_up_the_great_waterfall
		}

		provinces_to_highlight = {
		}

		allow = {
			NOT = { 
				has_country_flag = deranne_dtc_menu_open
			}
		}

		effect = {
			set_country_flag = deranne_dtc_menu_open
			country_event = {
				id = flavour_deranne.23
			}
		}
	}
}