country_decisions = {
	seek_new_form = {
		major = yes 
		potential = {
			has_estate_privilege = estate_nobility_patrons_of_the_lodges
		}

		provinces_to_highlight = {
			has_province_modifier = shifter_lodge_active
		}

		allow = {
			army_tradition = 10 
			custom_trigger_tooltip = {
				tooltip = seek_new_form_trigger_tooltip
				NOT = {
					has_country_modifier = shifter_change_substype_dummy_cooldown
					has_country_flag = shifter_no_open_descion_while_event_open
				}
			}
			calc_true_if = {
				amount = 3
				all_owned_province = {
					has_province_modifier = shifter_lodge_active
				}
			}
		}

		effect = {
			country_event = {
				id = sarhalevents.402 
			}
		}
		
	}

}