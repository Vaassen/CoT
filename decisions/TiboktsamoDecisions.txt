country_decisions = {
	L53_seek_ghost_halflings = {
		major = yes
		potential = {
			tag = L53
			any_owned_province = {
				has_province_modifier = L53_eloko_sighting
			}
		}
		provinces_to_highlight = {
			region = dao_nako_region
			any_province = {
				has_province_modifier = L53_eloko_sighting
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			any_owned_province = {
				has_province_modifier = L53_eloko_sighting
				owned_by = ROOT
				controlled_by = ROOT
				num_of_units_in_province = {
					who = ROOT
					amount = 10
				}
			}
		}
		effect = {
			random_owned_province = {
				limit = {
					has_province_modifier = L53_eloko_sighting
					owned_by = ROOT
					controlled_by = ROOT
					num_of_units_in_province = {
						who = ROOT
						amount = 10
					}
				}
				remove_province_modifier = L53_eloko_sighting
				spawn_rebels = {
					type = independence_rebels
					culture = this
					size = 1
				}
				change_religion = nakavy_avo
				change_culture = root
			}

			if = { #First decision use.
				limit = {
					NOT = { has_country_flag = L53_first_ghost_search_completed }
				}
				country_event = {
					id = flavour_tiboktsamo.4 #Another form of cowardice event
				}
			}

			if = { #Last decision use.
				limit = {
					dao_nako_region = {
						type = all
						NOT = { has_province_modifier = L53_eloko_sighting }
					}
				}
				country_event = {
					id = flavour_tiboktsamo.5 # Ghost Stories event.
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 300
	}

	L53_begin_the_duel = {
		major = yes
		potential = {
			tag = L53
			mission_completed = a_brothers_betrayal
		}
		provinces_to_highlight = {

		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			custom_trigger_tooltip = {
				tooltip = win_battle_tiboktsamo_10_tooltip
				check_variable = {
					which = tiboktsamoBattlesWon
					value = 10
				}
			}
			custom_trigger_tooltip = {
				tooltip = win_war_tiboktsamo_5_tooltip
				check_variable = {
					which = tiboktsamoWarsWon
					value = 5
				}
			}
		}
		effect = {
			country_event = {
				id = flavour_tiboktsamo.9
			}
			hidden_effect = {
				set_country_flag  = L53_completed_duel
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 300
	}

	# L53_test_decision = { #FOR TESTING PURPOSES ONLY, TAKE THIS OUT OF COMPLETED TREE!
	# 	major = yes
	# 	potential = {
	# 		tag = L53
	# 		was_player = yes
	# 	}
	# 	provinces_to_highlight = {

	# 	}
	# 	allow = {

	# 	}
	# 	effect = {
	# 		set_government_rank = 3
	# 		dao_nako_region = {
	# 			cede_province = L53
	# 			add_core = L53
	# 		}
	# 		horashesh_region = {
	# 			cede_province = L53
	# 			add_core = L53
	# 		}
	# 	}
	# }
}