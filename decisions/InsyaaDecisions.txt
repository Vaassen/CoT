
country_decisions = {

	ritual_burning = {
		potential = {
			has_reform = yenbenmirra_tribe
			NOT = { has_country_flag = formed_bomowoka_flag }
			NOT = { exists = P18 }
		}
		allow = {
			has_reform = yenbenmirra_tribe
			treasury = 50
			is_at_war = no
			hidden_trigger = { ai = no }
			oceania = {
                tribal_land_of = ROOT
                NOT = { is_capital_of = ROOT }
            }
		}
		effect = {
			custom_tooltip = yenbenmirra_ritual_burning_tt
			add_treasury = -50
			if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 8
					}
				}
				add_church_power = 200
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 7
					}
				}
				add_church_power = 175
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 6
					}
				}
				add_church_power = 150
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 5
					}
				}
				add_church_power = 125
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 4
					}
				}
				add_church_power = 100
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 3
					}
				}
				add_church_power = 75
			}
			else_if = {
				limit = {
					calc_true_if = {
						oceania = {
							type = all
							tribal_land_of = ROOT
							NOT = { is_capital_of = ROOT }
						}
					amount = 2
					}
				}
				add_church_power = 50
			}
			else = {
				add_church_power = 25
			}
			hidden_effect = {
				if = {
					limit = {
						NOT = { num_of_cities = 2 }
					}
					capital_scope = { add_devastation = 50 }
				}
				every_tribal_land_province = {
					limit = {
						NOT = {	is_capital_of = ROOT } # This is so you actually have to migrate around
					}
					add_devastation = 25
					change_tribal_land = ---
				}
			}
		}
	}
	
	yenbenmirra_settle_down = {
		major = yes
		potential = {
			has_reform = yenbenmirra_tribe
			exists = P18
		}
		allow = {
			has_reform = yenbenmirra_leaders_of_fire
			num_of_cities = {
				value = 10
			}
			has_institution = feudalism
			is_at_war = no
		}
		effect = {
			change_government = monarchy
			add_government_reform = yenbenmirra_federation
			hidden_effect = {
				remove_country_modifier = children_of_fire_collection_small
				remove_country_modifier = children_of_fire_collection_medium
				remove_country_modifier = children_of_fire_collection_large
				remove_country_modifier = children_of_fire_collection_huge
				remove_country_modifier = children_of_fire_collection_massive
			}
			if = {
				limit = {
					religious_school = {
						group = danguranga_religion
						school = danguranga_fire_water_school
					}
				}
				change_religion = mumambarra
			}
		}
	}
}