namespace =  cot_rp_events

country_event = { # THIS CAMPAIGN ONLY # CoT
    id = cot_rp_events.1
    title = cot_rp_events.1.t
    desc = cot_rp_events.1.d
    picture = BIG_STAR_IN_SKY_eventPicture

    fire_only_once = yes

    trigger = {
        tag = Z15
        NOT = { has_country_flag = cot_reward }
    }

    option = {
		Z28 = {
			define_ruler = {
				name = "Rolf"
				dynasty = "Ebonfrost"
				age = 27
				religion = elikhetist
				culture = olavish
				adm = 4
				dip = 3
				mil = 4
			}
			add_ruler_personality = tolerant_personality
			add_ruler_personality = strict_personality
		}
        set_country_flag = cot_reward
		add_adm_power = 50
		add_adm_tech = 1
		add_dip_tech = 2
		add_idea_group = mil2
		add_idea = mil2_7_a
	}
}

country_event = { # THIS CAMPAIGN ONLY # CoT
    id = cot_rp_events.9
    title = cot_rp_events.9.t
    desc = cot_rp_events.9.d
    picture = BIG_STAR_IN_SKY_eventPicture

    fire_only_once = yes

    trigger = {
        tag = A32
        ruler_age = 75
    }

    option = {
		name = cot_rp_events.9.a
		A32 = {
			define_consort = {
				age = 75
				culture = sea_elf 
				name = "Myrmien"
				adm = 5
				dip = 5
				mil = 1
		    }   
			remove_country_modifier = CoT_adrahel_underage
			teagansfield_area = {
				change_culture = sea_elf 
				add_province_modifier = {
					name = CoT_adrahel_sea_elves
					duration = -1
				}
			}
			add_prestige = -25
	    }
    }
}

country_event = { # THIS CAMPAIGN ONLY # CoT
    id = cot_rp_events.10
    title = cot_rp_events.10.t
    desc = cot_rp_events.10.d
    picture = BIG_STAR_IN_SKY_eventPicture

    fire_only_once = yes

    trigger = {
        esmaria_region = {
			OR = {
				is_core = Z46
				owner = {
					has_reform = free_city
				}
			}
		}
    }

    option = {
		name = cot_rp_events.10.a
		Z46 = {
			every_owned_province = {
				add_core = A32
			}
		}
		A32 = {
			increase_ruler_adm_effect = yes
			increase_ruler_dip_effect = yes
			increase_ruler_mil_effect = yes
		}
    }
}

