

namespace = flavor_pearlsedge

#Will they convert?
country_event = {
	id = flavor_pearlsedge.1
	title = flavor_pearlsedge.1.t
	desc = flavor_pearlsedge.1.d
	picture = {
		trigger = {
			NOT = { has_dlc = "Rights of Man" }
		}
		picture = COURT_eventPicture
	}
	
	fire_only_once = yes
	is_triggered_only = yes

	option = {		
		name = "flavor_pearlsedge.1.a" #we can no longer blindly serve
		ai_chance = { factor = 10 }	
		add_prestige = 10
		declare_war_with_cb = {
			who = FROM
			casus_belli = cb_independence_war
		}
	}
	
	option = {		
		name = "flavor_pearlsedge.1.b" #set them right
		ai_chance = { factor = 10 }	
		add_devotion = 10
		declare_war_with_cb = {
			who = FROM
			casus_belli = cb_crusade
		}
	}
	
	option = {		
		name = "flavor_pearlsedge.1.c" #We obey
		ai_chance = { factor = 10 }	
		change_religion = infernal_court
		capital_scope = {
			change_religion = infernal_court
		}
		add_country_modifier = {
			name = "conversion_zeal"
			duration = 3650
		}
		hidden_effect = {
			add_prestige = 100 #offsets the -100 for converting
		}
		add_prestige = -25 #visible so they know they'll lose 25
	}
}