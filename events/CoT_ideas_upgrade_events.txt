#Form a new country
country_event = {
	id = cot_ideas_upgrade.2
	title = ideagroups.1.t
	desc = ideagroups.1.d
	picture = ADVISOR_eventPicture

	is_triggered_only = yes

    #Lorent
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = A01 }                 # LORENT

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_lorent_3   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}
	
    #Vinlorn
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = W14 }                 # Vinlorn

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_vinlorn_2   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}
	
    #Rosica
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = W16 }                 # Rosica

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_rosica_2   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}
	
    #Sorncost
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = A09 }                 # Sorncost

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_sorncosti_2   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}

	#Roilsard
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = A78 }                 # Roilsard

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_roilsardi_2   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}

	#Carneter
	option = {
		name = ideagroups.1.a	#Yes Please!
		trigger = { tag = B78 }                 # Carneter

		clr_CoT_ideas = yes                   # CLEAR THE OLD FLAGS
        set_country_flag = CoT_ideas_carnetori_2   # SET THE NEW FLAG
		swap_free_idea_group = yes		        # REPLACE THE IDEAS ACCORDING TO THE NEW FLAGS

		ai_chance = { factor = 1 }
	}

	option = {
		name = ideagroups.1.b	#No, we cannot abandon our roots.

		ideas_bump = yes 				        # BUMP THE IDEAS
		if = {
			limit = {
				OR = {			# BUMP IDEAS AGAIN IF FORMING T3 TAG
					tag = A01	# LORENT
					tag = A13	# GAWED
				}
			}
			ideas_bump = yes
		}
		swap_free_idea_group = yes		        # REPLACE THE IDEAS WITH UPGRADED ONES

		ai_chance = { factor = 0 }
	}
}