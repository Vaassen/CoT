sed_outpost_phase_i = {
	potential = { always = yes }

	trigger = {
		OR = {
			local_autonomy = 80.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}
	
	min_local_autonomy = -20

	on_deactivation = {
		sedentarization_outpost_try_phase_change = { current_phase_is_one = yes }
	}
}

sed_outpost_phase_ii = {
	potential = { always = yes }

	trigger = {
		OR = {
			local_autonomy = 65.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}
	
	min_local_autonomy = -35

	on_deactivation = {
		sedentarization_outpost_try_phase_change = { current_phase_is_two = yes }
	}
}

sed_outpost_phase_iii = {
	potential = { always = yes }

	trigger = {
		OR = {
			local_autonomy = 50.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}
	
	min_local_autonomy = -50

	on_deactivation = {
		sedentarization_outpost_try_phase_change = { current_phase_is_three = yes }
	}
}