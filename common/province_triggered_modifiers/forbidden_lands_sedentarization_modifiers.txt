sed_triggered_outpost_phase_i = {
	hidden = yes
	potential = { always = yes }
	trigger = {
		OR = {
			local_autonomy = 80.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}

	on_deactivation = {
		# TODO: think about factoring this into an event instead; may fix the startup error spam
		sedentarization_outpost_try_phase_change = { current_phase_is_one = yes }
	}
}

sed_triggered_outpost_phase_ii = {
	hidden = yes
	potential = { always = yes }
	trigger = {
		OR = {
			local_autonomy = 65.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}

	on_deactivation = {
		sedentarization_outpost_try_phase_change = { current_phase_is_two = yes }
	}
}

sed_triggered_outpost_phase_iii = {
	hidden = yes
	potential = { always = yes }
	trigger = {
		OR = {
			local_autonomy = 50.1
			has_province_modifier = sed_outpost_progress_blocked
		}
	}

	on_deactivation = {
		sedentarization_outpost_try_phase_change = { current_phase_is_three = yes }
	}
}