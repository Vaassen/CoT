charkuchin_kobold_citadel_supplies_mechanic = {
	alert_icon_gfx = GFX_alerticons_government_mechanics
	alert_icon_index = 14 # No way to use the power so doesn't matter
	available = {
		OR = { #Apparently this loads before scripted triggers so it writes to the error log if I use charkuchin_kobold_culture, though it does work
			culture = yawasichya_kobold
			culture = jatukyu_kobold
			culture = unitsan_kobold
		}
	}

	powers = {
		citadel_supplies = {
			gui = ashhana_blood_curse_bar #placeholder
			max = 1000
			min = 0
			default = 10
			reset_on_new_ruler = no
			base_monthly_growth = 0
			is_good = yes
			
			range_modifier = {
				trigger = { }
				modifier = {
					global_colonial_growth = -500 #xxx not sure what number
				}
				start_value = 0
				end_value  = 0
			}
			on_min_reached = { country_event = { id = charkuchin_kobold.12 } } # This event starts reducing dev on provinces and decolonising them
		}
	}
	
	interactions = {
		# This is just so we can have an alert icon when supplies are low. Unfortunately I can't stop the tooltip from saying
		# "Interaction Available" but we'll see if that's okay
		low_supplies = {
			icon = GFX_blank_icon1x1
			gui = dummy_hold_gov_mech
			trigger = {
				NOT = {
					has_government_power = {
						mechanic_type = charkuchin_kobold_citadel_supplies_mechanic
						power_type = citadel_supplies
						value = 500
					}
				}
				NOT = {
					check_variable = {
						which = insyaa_charkuchin_kobold_citadel_supplies_total_gain
						which = insyaa_charkuchin_kobold_citadel_supplies_total_cost
					}
				}
			}
			effect = {
			}
		}
	}
}