wos_kobolds_with_a_dragon = {

	color = { 230 175 175 }

	area = nation
	government = any
	religion = kobold_dragon_cult
	defection = none	# Will not defect.
	defect_delay = 999			# Held provinces will auto-defect after 18 months, regardless of accept/enforce demands.
	independence = none	#What will gain independence (note: ai does not seem to get these rebels if not set to 'none')
	unit_transfer = no		# | Units switch allegiance after enforcing demands.
	gfx_type = culture_owner
	will_relocate = yes #| Units will relocate if they have sieged all provinces nearby and has to move over sea.
	
	resilient = yes 
	reinforcing = yes			#yes/no | whether the rebels reinforce themselves
	general = yes			#yes/no | whether they have a general
	smart = yes			#yes/no | Does, or does not assault forts, tactically withdraw from battle, etc.
	
	artillery = 0 	#decimal percentage of army makeup
	infantry = 1
	cavalry = 0
	
	morale = 1.5

	# Possible handle actions
	handle_action_negotiate = no
	handle_action_stability = no
	handle_action_build_core = no
	handle_action_send_missionary = no	
	
	# The rebel type with the highest modifier for this province gets picked
	spawn_chance = {
		factor = 0
	}
 
 	# This is checked for EACH province in the Area of Operations
	movement_evaluation = {
		factor = 0
	}
 
 	# Province Scope
 	siege_won_trigger = {
		<trigger>
 	}
	siege_won_effect = {
		<effects>
	}
	
	# Country scope
	can_negotiate_trigger = {	#whether the player is allowed to negotiate
		ai = yes
	}
	
	# Country scope

	
	# Localisation for their demands
	demands_description = "Kobold Rule"
	
	# Country Scope	
	demands_enforced_effect = { #all that work and it doesn't work
		I34 = {
			all_owned_province = {
				change_culture = darkscale_kobold
				change_religion = kobold_dragon_cult
				decolonize = this
			}
		}
	}
}