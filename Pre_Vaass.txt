# Execute Before running Vaass.txt, sets needed flags and effects.

# Gives Esmaria their idea flags
A95 = {
    set_country_flag = CoT_ideas_esmaria
    set_country_flag = CoT_ideas_tier_3
    clear_country_flag = CoT_ideas_themarenn
    clear_country_flag = CoT_ideas_tier_1
    change_tag = Z46
}

Z46 = {
    if = {
		limit = { has_custom_ideas = no }
		country_event = { id = ideagroups.1 } #Swap Ideas
	}
}